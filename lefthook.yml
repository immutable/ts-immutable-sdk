# https://lefthook.dev/configuration/
pre-commit:
  commands:
    oxlint:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
      exclude:
        - "docs/**"
      run: NODE_OPTIONS="--max-old-space-size=10240" pnpm exec oxlint --no-error-on-unmatched-pattern {staged_files}
