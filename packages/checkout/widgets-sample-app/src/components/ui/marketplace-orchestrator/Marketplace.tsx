import { MainPage } from './MainPage';
import { WidgetProvider } from './WidgetProvider';

export const Marketplace = () => {

  return (
    <WidgetProvider>
      <MainPage />
    </WidgetProvider>
  );
};
